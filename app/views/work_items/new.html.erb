<h1>New work_item</h1>

<%= form_for([@report, @work_item]) do |f| %>
  <% if @work_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_item.errors.count, "error") %> prohibited this work_item from being saved:</h2>

      <ul>
      <% @work_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', @report %>
