<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :month %><br />
    <%= f.select :month, Report::MONTHS %>
  </div>
  <div class="field">
    <%= f.label :year %><br />
    <%= f.select :year, Report::YEARS %>
  </div>
  <div class="field">
    <%= f.label :revenue %><br />
    <%= f.text_field :revenue %>
  </div>
  <div class="field">
    <%= f.label :expense %><br />
    <%= f.text_field :expense %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>

  <%= f.hidden_field :project_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
